# Trustworthiness Detector Configuration
# Copy this file to .env and add your API keys

# ============================================
# STEP 1: Add your API key(s)
# You only need ONE of these keys
# ============================================

# For OpenAI models (gpt-3.5-turbo, gpt-4)
OPENAI_API_KEY=your-openai-key-here

# For Google models (gemini/gemini-pro) - RECOMMENDED for this assignment
# Get free key at: https://ai.google.dev/gemini-api/docs/api-key
GEMINI_API_KEY=your-gemini-key-here

# For Anthropic models (claude-2, claude-3)
ANTHROPIC_API_KEY=your-anthropic-key-here

# ============================================
# STEP 2: Set your default model
# This model will be used unless specified otherwise
# Make sure you have the API key for this model!
# ============================================

# Recommended: Use Gemini (free API)
DEFAULT_MODEL=gemini/gemini-pro

# Other options:
# DEFAULT_MODEL=gpt-3.5-turbo
# DEFAULT_MODEL=gpt-4
# DEFAULT_MODEL=claude-2

# ============================================
# How it works:
# - All code uses DEFAULT_MODEL automatically
# - No need to change model in multiple files
# - Just update this one setting
# ============================================

# Optional: API Configuration
# -------------------------------
# API host and port (default: 0.0.0.0:8000)
HOST=0.0.0.0
PORT=8000

# Logging Level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Security
# -------------------------------
# Secret key for JWT token encryption
SECRET_KEY=your_secret_key_here
# Token expiration time in minutes
ACCESS_TOKEN_EXPIRE_MINUTES=1440  # 24 hours

# CORS (Cross-Origin Resource Sharing)
# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:3000,http://localhost:8000

# Rate Limiting
# -------------------------------
# Maximum requests per minute per IP
RATE_LIMIT=100

# Database (if applicable)
# -------------------------------
# DATABASE_URL=postgresql://user:password@localhost:5432/trustworthydb

# Monitoring
# -------------------------------
# Prometheus metrics endpoint (default: /metrics)
PROMETHEUS_METRICS_PATH=/metrics

# Optional: Third-party Integrations
# -------------------------------
# SENTRY_DSN=your_sentry_dsn_here
# SLACK_WEBHOOK_URL=your_slack_webhook_url_here
